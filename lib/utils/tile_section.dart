import 'package:flutter/material.dart';

class TileSection {
  String title;
  Color color;
  IconData icon;
  int numbers;

  TileSection(this.title, this.color, this.icon, this.numbers);
}
